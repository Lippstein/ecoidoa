{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo Front-end usa Bootstrap via CDN, sem build Vite.",
        "dev": "echo Front-end usa Bootstrap via CDN, sem servidor Vite."
    },
    "devDependencies": {
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "sass": "^1.93.2"
    },
    "dependencies": {
        "@popperjs/core": "^2.11.8",
        "bootstrap": "^5.3.8",
        "sweetalert2": "^11.23.0"
    }
}
